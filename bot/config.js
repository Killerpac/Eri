require('dotenv').config()
module.exports = {
    discord: {
        activity:"Music Discovery",
        token: process.env.TOKEN,
        cookie: process.env.COOKIE,
        ne:[]
    },
};
